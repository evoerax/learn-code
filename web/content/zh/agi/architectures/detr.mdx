---
title: DETR
description: 'Detection Transformer 端到端检测'
---

# DETR

基于 Transformer 的端到端检测。

---

## 架构

```python
class DETR(nn.Module):
    def __init__(self, num_classes=91):
        super().__init__()
        self.backbone = ResNet50()
        self.transformer = Transformer(d_model=256, nhead=8, num_layers=6)
        self.query_embed = nn.Embedding(num_queries, 256)
        self.class_embed = nn.Linear(256, num_classes + 1)  # +1 for no object
        self.bbox_embed = nn.Sequential(
            nn.Linear(256, 256),
            nn.ReLU(),
            nn.Linear(256, 4)
        )
    
    def forward(self, x):
        # CNN 特征
        feat = self.backbone(x)
        
        # Transformer 解码
        hs = self.transformer(
            self.query_embed.weight.unsqueeze(1).repeat(1, B, 1),
            feat.flatten(2).permute(2, 0, 1)
        )
        
        # 预测
        pred_classes = self.class_embed(hs)
        pred_boxes = self.bbox_embed(hs).sigmoid()
        
        return pred_classes, pred_boxes
```

---

## 集合预测

```python
# N=100 个查询, 预测 N 个框
# 无需 NMS, 无需 anchor
```

---

## 损失函数

```python
# Hungarian 匹配
# 预测与 GT 最佳匹配
matcher = HungarianMatcher()
idx = matcher(pred_boxes, gt_boxes)

# 损失
loss = loss_class(pred[idx], gt_labels[idx]) + \
       loss_box(pred[idx], gt_boxes[idx])
```

---

## 优缺点

| 优点 | 缺点 |
|------|------|
| 端到端 | 小物体检测差 |
| 无 NMS | 训练慢 |
| 全局注意力 | 收敛慢 |
