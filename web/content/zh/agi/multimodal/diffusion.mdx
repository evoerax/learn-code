---
title: 扩散模型
description: 'Diffusion Models 基础'
---

# 扩散模型

通过逐步加噪和去噪生成图像。

---

## 前向过程

```python
def forward_diffusion(x_0, t):
    # 逐步加噪
    # q(x_t | x_{t-1}) = N(x_t; sqrt(1-beta_t) * x_{t-1}, beta_t * I)
    noise = torch.randn_like(x_0)
    alphas_bar = torch.cumprod(1 - betas, dim=0)
    x_t = torch.sqrt(alphas_bar[t]) * x_0 + torch.sqrt(1 - alphas_bar[t]) * noise
    return x_t
```

---

## 反向过程

```python
class UNet(nn.Module):
    def __init__(self):
        self.down = nn.ModuleList([...])  # 编码器
        self.up = nn.ModuleList([...])    # 解码器
    
    def forward(self, x, t):
        # t 是时间步 embedding
        t_embed = time_embedding(t)
        # ... 编码解码
        return noise_pred
```

---

## 训练目标

```python
def diffusion_loss(model, x_0):
    # 随机时间步
    t = torch.randint(0, T, (B,))
    
    # 加噪
    x_t = forward_diffusion(x_0, t)
    
    # 预测噪声
    noise_pred = model(x_t, t)
    noise = torch.randn_like(x_0)
    
    # MSE 损失
    return F.mse_loss(noise_pred, noise)
```

---

## 采样

```python
def sample(model, shape):
    x = torch.randn(shape)
    for t in reversed(range(T)):
        # 逐步去噪
        x = model(x, t)
    return x
```
