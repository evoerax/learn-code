---
title: ViT Vision Transformer
description: '视觉 Transformer'
---

# ViT Vision Transformer

ViT (Dosovitskiy et al., 2021) 将 Transformer 架构直接应用于图像块。

---

## 架构

```python
class ViT(nn.Module):
    def __init__(self, image_size=224, patch_size=16, dim=768, num_heads=12, num_layers=12):
        super().__init__()
        self.patch_size = patch_size

        # 分块嵌入
        num_patches = (image_size // patch_size) ** 2
        self.patch_embed = nn.Conv2d(3, dim, patch_size, patch_size)

        # CLS token
        self.cls_token = nn.Parameter(torch.randn(1, 1, dim))

        # 位置嵌入
        self.pos_embed = nn.Parameter(torch.randn(1, num_patches + 1, dim))

        # Transformer 块
        self.blocks = nn.ModuleList([
            TransformerBlock(dim, num_heads) for _ in range(num_layers)
        ])

        # 分类头
        self.head = nn.Linear(dim, num_classes)

    def forward(self, x):
        # 分块: (B, 3, 224, 224) -> (B, 196, 768)
        x = self.patch_embed(x).flatten(2).transpose(1, 2)

        # 添加 CLS token
        cls_tokens = self.cls_token.expand(x.size(0), -1, -1)
        x = torch.cat([cls_tokens, x], dim=1)

        # 添加位置嵌入
        x = x + self.pos_embed

        # Transformer 块
        for block in self.blocks:
            x = block(x)

        # 从 CLS token 分类
        return self.head(x[:, 0])
```

---

## 分块嵌入

```
图像 (224x224, RGB)
        │
        ▼
┌───┬───┬───┬───┐
│ P │ P │ P │ P │  16x16 块
├───┼───┼───┼───┤  → 展平 → 196 个 768维块
│ P │ P │ P │ P │
├───┼───┼───┼───┤
│ P │ P │ P │ P │
└───┴───┴───┴───┘
```

---

## vs CNN

| 方面 | ViT | CNN |
|------|-----|-----|
| **归纳偏置** | 低（块级别） | 高（局部性） |
| **数据需求** | 更多 | 更少 |
| **计算量** | 小模型更高 | 更低 |
| **微调** | 灵活 | 可能需要重新设计 |
| **注意力** | 从开始就是全局 | 渐进式 |
