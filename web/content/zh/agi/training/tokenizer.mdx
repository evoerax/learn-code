---
title: Tokenizer 与 BPE
description: '分词器与 Byte Pair Encoding'
---

# Tokenizer 与 BPE

分词器将原始文本转换为 LLM 可处理的 token 序列。BPE（Byte Pair Encoding）是最常用的子词分词算法。

---

## BPE 算法

```python
def learn_bpe(vocab_size=32000):
    # 从字符级词表开始
    vocab = {chr(i): i for i in range(256)}

    # 统计 token 对的出现频率
    def get_pairs(word_counts):
        pairs = defaultdict(int)
        for word, freq in word_counts.items():
            symbols = word.split()
            for i in range(len(symbols) - 1):
                pairs[(symbols[i], symbols[i+1])] += freq
        return pairs

    # 迭代合并最频繁的 token 对直到达到词表大小
    while len(vocab) < vocab_size:
        pairs = get_pairs(word_counts)
        best = max(pairs, key=pairs.get)
        vocab[merge(best)] = len(vocab)
        word_counts = merge_pair(best, word_counts)

    return vocab
```

---

## 分词流程

```python
def tokenize(text, vocab):
    tokens = list(text.encode('utf-8'))

    while len(tokens) >= 2:
        # 查找最佳合并对
        pairs = [(tokens[i], tokens[i+1]) for i in range(len(tokens)-1)]
        pair_scores = [(vocab.get(pair, 0), i, pair) for i, pair in enumerate(pairs)]
        best = max(pair_scores, key=lambda x: x[0])[1]

        if vocab.get(pairs[best], 0) == 0:
            break

        tokens = tokens[:best] + [merge(pairs[best])] + tokens[best+2:]

    return tokens
```

**分词示例：**
```
原始文本: "LLM is amazing"
        ↓
Unicode: ['L', 'L', 'M', ' ', 'i', 's', ...]
        ↓
BPE 合并: ['LLM', 'Ġis', 'Ġam', 'az', 'ing']
```

---

## 常用分词器对比

| 分词器 | 词表大小 | 语言 | 算法 | 备注 |
|--------|----------|------|------|------|
| **GPT-2** | 50,257 | 英文 | BPE | OpenAI 标准 |
| **GPT-4** | ~100K | 多语言 | BPE 变体 | 支持更广语言 |
| **Claude** | ~64K | 多语言 | SentencePiece | Anthropic |
| **Llama** | 32,000 | 多语言 | SentencePiece | Meta |
| **Qwen** | 151,936 | 中文为主 | BPE | 阿里 |

---

## tiktoken 示例

```python
import tiktoken
enc = tiktoken.get_encoding("cl100k_base")
tokens = enc.encode("Hello, world!")
# [15496, 11, 1917, 0]
```
